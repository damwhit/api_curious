<div class="container">
  <div class="row profile">
    <div class="col-md-4 col-md-offset-2 %>">
      <%= image_tag @user.image_url, class: "img-circle prof-img"%>
    </div>
    <div class="col-md-4 col-md-offset-1">
      <h1><%= @user.username %></h1>
      <h3><%= @user.name %></h3>
      <table class="table">
        <tr>
          <td>
            <%= @info.counts[:media] %> posts
          </td>
          <td>
            <%= @info.counts[:followed_by] %> followers
           </td>
           <td>
            <%= @info.counts[:follows] %> following
           </td>
        </tr>
      </table>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
    <% @media.each do |media| %>
      <div class="col-md-4 col-md-offset image-div" >
        <div class="row">
          <div class="col-xs-12">
            <%= image_tag(media[:images][:low_resolution][:url], class: "user-pics") %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
